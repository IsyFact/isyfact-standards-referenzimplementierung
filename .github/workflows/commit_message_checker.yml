name: Commit Message Checker
on:
  pull_request:
    branches:
      - main
      - 'release/**'

jobs:
  check-commit-message:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/commit_message_checker_template.yml@v2.6.0
